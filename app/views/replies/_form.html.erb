<%= form_with(model: reply, local: true) do |form| %>
  <% if reply.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reply.errors.count, "error") %> prohibited this reply from being saved:</h2>

      <ul>
        <% reply.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container">
    <div class="col-md-4 col-md-offset-3">
      <div class="row form">

        <div class="field form-group">
          <%= form.label :message %>
          <%= form.text_area :message, id: :reply_message %>
        </div>

        <div class="actions form-group">
          <%= form.submit 'Post Reply', class: 'btn btn-primary' %>
        </div>

      </div>
    </div>
  </div>
<% end %>
